using System;
using System.Collections.Generic;
using System.Text;

namespace Omnia.CLI
{
    public enum StatusCodes
    {
        Success = 0,
        UnknownError = -1,
        InvalidOperation = 2,
        InvalidArgument = 3
    }
}
